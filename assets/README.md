# assets/

Everything `index.html` links to that isn't content.

| Folder | Contains |
|--------|----------|
| [`css/`](css/README.md) | Stylesheet. |
| [`js/`](js/README.md) | Renderer app. |
| [`images/`](images/README.md) | Portrait, book covers, `og.png`. |

`css/` and `js/` are code, MIT-licensed like `index.html`. `images/` is not — see the root
[README](../README.md#licence).
